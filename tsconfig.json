{
  "extends": "astro/tsconfigs/strictest",
  "compilerOptions": {
    // Note: mixing Node and browser types could be dangerous, but it's the recommended approach for Astro
    "types": ["unplugin-icons/types/astro", "node"]
  }
}
